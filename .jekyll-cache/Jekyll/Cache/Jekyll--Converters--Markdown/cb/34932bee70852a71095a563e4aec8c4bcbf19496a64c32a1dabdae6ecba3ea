I"<h2 id="structure">Structure</h2>

<p>Each artifact is defined in a file in the <a href="https://github.com/dfirtips/evids.dfir.tips/blob/master/_evids/"><code class="language-plaintext highlighter-rouge">_evids/</code></a> folder named as <code class="language-plaintext highlighter-rouge">&lt;artifact&gt;.md</code>, such file consists only of a <a href="http://yaml.org/">YAML</a> front matter which describes the binary and its functions.</p>

<p>The full syntax is the following:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
title: Title of the artifact entry
description: |
    Multiline description
location: |
    Multiline location
interpretation: |
    Multiline intepretation
evids-categories:
    - CATEGORY
---
</code></pre></div></div>

<p>Where <code class="language-plaintext highlighter-rouge">CATEGORY</code> is one of the values described in the <a href="https://github.com/dfirtips/evids.dfir.tips/blob/master/_data/evids-categories.yml"><code class="language-plaintext highlighter-rouge">_data/evids-categories.yml</code></a> file.</p>

<p>Feel free to use any file in the <a href="https://github.com/dfirtips/evids.dfir.tips/blob/master/_evids/"><code class="language-plaintext highlighter-rouge">_evids/</code></a> folder as an example.</p>

<h2 id="pull-request-process">Pull request process</h2>

<p>There are no upfront requirements of what new entry should or should not be about. Each pull request will be evaluated individually.</p>

<p>Pull requests adding new functions in <a href="https://github.com/dfirtips/evids.dfir.tips/blob/master/_data/evids-categories.yml"><code class="language-plaintext highlighter-rouge">_data/evids-categories.yml</code></a> are allowed and subjected to project maintainers vetting.</p>

:ET