---
title: Evids

exclude: ['/scripts', '/Gemfile', '/Makefile', '/README.md', '/CONTRIBUTING.md']

permalink: pretty

collections:
  evids:
    output: true

defaults:
  - scope:
      path: '_evids'
    values:
      layout: artifact

dfirurl: evids
